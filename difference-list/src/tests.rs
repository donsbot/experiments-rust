#[cfg(test)]

use dlist::{id};

mod tests {
    #[test]
    fn test_id1() {
/*
        assert_eq!(dlist::id(1), 1);
        assert_eq!(dlist::id(()), ());
        assert_eq!(dlist::id(""), "");
        assert_eq!(dlist::id([1,2,3]), [1,2,3]);
*/
    }
}
